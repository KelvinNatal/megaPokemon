import { types } from "./types"

export type data =  {
    sprites: {
        front_default: string
        back_default: string
        front_shiny: string
    }
    types: types[]
    name: string
    height: number
}